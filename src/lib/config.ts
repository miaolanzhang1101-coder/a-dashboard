// Property-level assumptions surfaced in plain language on the dashboard.
export const HOTEL_NAME = "Marlow House";
export const ROOMS = 74;
// What an online travel agency would have charged on these stays. Used only
// to express "commission avoided" — change to match the property's OTA contracts.
export const OTA_COMMISSION_RATE = 0.15;
